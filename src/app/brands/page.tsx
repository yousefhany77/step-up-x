import BrandsList from '@components/Brands/BrandsList'

const page = ({}) => {
  return (
    <section
      style={{
        minHeight: 'calc(100vh - 200px)',
      }}
    >
      <BrandsList />
    </section>
  )
}

export default page
