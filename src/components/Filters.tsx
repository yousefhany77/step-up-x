'use client'
import { Box } from '@mantine/core'

function Filters() {
  return (
    <Box
      sx={(theme) => ({
        width: '100%',
        height: '100%',
        backgroundColor: theme.colors.gray[2],
      })}
    >
      Filters
    </Box>
  )
}

export default Filters
